# Optional: repo roots for Invoke-FindRepo / repo alias (supports ';' or ',' separators)
# $env:GitRepoRootDir = "C:\Git;D:\Work\Repos"
# Optional: max search depth for repo discovery (default: 4)
# $env:GitRepoSearchDepth = "6"
